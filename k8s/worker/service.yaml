apiVersion: v1
kind: Service
metadata:
  name: tempos-worker
  namespace: tempos
  labels:
    app: tempos-worker
spec:
  type: ClusterIP
  selector:
    app: tempos-worker
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
